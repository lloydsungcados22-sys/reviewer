# Lightweight requirements.txt for Streamlit Cloud if full version fails
# Use this if torch/torchvision installation fails due to size limits

pandas>=2.0.0
pdfplumber>=0.10.0
PyMuPDF>=1.23.0
pytesseract>=0.3.10
python-docx>=1.0.0
reportlab>=4.0.0
snowflake-connector-python>=3.0.0
streamlit>=1.28.0
Pillow>=10.0.0
openai>=1.12.0
numpy>=1.24.0
opencv-python-headless>=4.8.0

# Note: EasyOCR requires torch/torchvision which are very large (~2GB)
# If you need OCR, you'll need to use the full requirements.txt
# Or EasyOCR will install torch automatically but may fail on Streamlit Cloud Community tier
